{% extends 'base.html.twig' %}
{% block title %}Publier une note interne{% endblock %}
{% block body %}

<div class="main-content-inner">
    <div class="row">
        <div class="col-12 mt-5">
            <div class="card">
                <div class="card-body">
                    <h1>Formulaire eto</h1>
                    {{ form_start(formNote) }}
                    {{ form_widget(formNote) }}
                    {{ form_end(formNote) }}
                </div>
            </div>
        </div>
    

        <div class="col-12 mt-5">
            <div class="card">
                <div class="card-body">
                    <h1>Ireo notes publiés eto</h1>
                </div>
            </div>
    </div>
    </div>
</div>
{% endblock %}
